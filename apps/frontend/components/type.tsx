// import { CoreMessage, CoreSystemMessage, CoreUserMessage, CoreAssistantMessage, CoreToolMessage } from 'ai';

// // Define a type that includes CoreMessage with additional properties
// export type CustomMessage =
//   | (CoreSystemMessage & { fileUrl?: string; })
//   | (CoreUserMessage & { fileUrl?: string; })
//   | (CoreAssistantMessage & { fileUrl?: string; })
//   | (CoreToolMessage & { fileUrl?: string; });
